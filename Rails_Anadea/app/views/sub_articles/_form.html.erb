<%= form_for [@article, @sub_article] do |f| %>
  <% if @sub_article.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@sub_article.errors.count, "error") %> prohibited this sub_article from being saved:</h2>

      <ul>
      <% @sub_article.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :content %><br>
    <%= f.text_area :content %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
